<%= form.input :content, as: :bootsy, label: false, input_html: {id: 'new_forum_post_content', :class => 'span8', rows: '8'}, bootsy_options: {upload: false} %>
<% if post.topic.watchings.where(user_id: current_user.id).empty? %>
  <%= render 'shared/watch_checkbox' %>
<% end %>