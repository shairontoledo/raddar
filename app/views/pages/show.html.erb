<% page_title @page.title %>

<% unless @page.title.blank? %>
  <div class="page-header">
    <h1><%= @page.title %></h1>
  </div>
<% end %>

<div class="page_content editable_content">
  <%= rich_format @page.content %>
</div>
