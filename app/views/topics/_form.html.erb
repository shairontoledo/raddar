<%= simple_form_for([@topic.forum,@topic], html: {:class => 'form-horizontal'}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, input_html: {:class => 'span4'} %>
    <% unless @topic.persisted? %>
      <%= f.simple_fields_for @post do |pf| %>
        <%= render 'posts/form_inputs', {form: pf, post: @post} %>
      <% end %>
    <% end %> 
  </div>

  <div class="form-actions">
    <%= submit_button f %>
    <%= destroy_button @topic.url if can? :destroy, @topic %>
  </div>
<% end %>
